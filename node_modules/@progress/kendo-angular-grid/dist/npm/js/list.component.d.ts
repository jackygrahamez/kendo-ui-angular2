import { OnInit, EventEmitter, OnDestroy, AfterViewInit, SimpleChange, OnChanges } from '@angular/core';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/observable/fromEvent';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/merge';
import 'rxjs/add/operator/filter';
import 'rxjs/add/operator/do';
import 'rxjs/add/operator/distinctUntilChanged';
import 'rxjs/add/operator/debounceTime';
import { ScrollerService, Action } from './scroller.service';
import { DetailTemplateDirective } from './detail-template.directive';
import { DetailsService } from './details.service';
import { ColumnsContainer } from './columns-container';
/**
 * @hidden
 */
export declare class ListComponent implements OnInit, OnDestroy, AfterViewInit, OnChanges {
    data: Array<any>;
    total: number;
    height: number;
    rowHeight: number;
    detailRowHeight: number;
    take: number;
    skip: number;
    columns: ColumnsContainer;
    detailTemplate: DetailTemplateDirective;
    selectable: boolean;
    pageChange: EventEmitter<Action>;
    containerScroll: EventEmitter<{
        scrollTop: number;
        scrollLeft: number;
    }>;
    totalHeight: number;
    style: any;
    private container;
    private lockedContainer;
    private scroller;
    private scrollSubscription;
    private resizeSubscription;
    private detailsSubscription;
    private containerScrollSubscription;
    private dispatcher;
    private rowHeightService;
    private PAGE_EVENT_TIMEOUT;
    private lockedColumns;
    private nonLockedColumns;
    private lockedWidth;
    private nonLockedWidth;
    constructor(scrollerFactory: (x: Observable<any>) => ScrollerService, detailsService: DetailsService);
    ngOnInit(): void;
    ngOnChanges(changes: {
        [propertyName: string]: SimpleChange;
    }): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    init(): void;
    protected detailExpand({index, expand}: {
        index: number;
        dataItem: any;
        expand: boolean;
    }): void;
    private scroll({offset});
    private onContainerScroll({scrollTop, scrollLeft});
}
