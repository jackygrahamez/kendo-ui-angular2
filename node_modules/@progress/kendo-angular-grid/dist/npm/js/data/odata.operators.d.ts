import { SortDescriptor } from '../sort-descriptor';
/**
 * Converts the data state of the Grid to an OData string.
 */
export declare const toODataString: (state: {
    skip: number;
    take: number;
    sort: SortDescriptor[];
}) => string;
