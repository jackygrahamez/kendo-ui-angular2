import { ConfigurationService } from '../../common/configuration.service';
import { SettingsComponent } from '../../common/settings.component';
/**
 * @hidden
 */
export declare abstract class ValueAxisCrosshairComponentGenerated extends SettingsComponent {
    configurationService: ConfigurationService;
    /**
     * The color of the crosshair. Accepts a valid CSS color string, including hex and rgb.
     */
    color: string;
    /**
     * The opacity of the crosshair. By default, the crosshair is opaque.
     */
    opacity: number;
    /**
     * If set to `true`, the Chart displays the value axis crosshair. By default, the value axis crosshair
     * is not visible.
     */
    visible: boolean;
    /**
     * The width of the crosshair in pixels.
     */
    width: number;
    /**
     * The crosshair tooltip options.
     *
     * The crosshair tooltip is displayed when the `valueAxis.crosshair.tooltip.visible` option is set to
     * `true`.
     */
    tooltip: any;
    constructor(configurationService: ConfigurationService);
}
