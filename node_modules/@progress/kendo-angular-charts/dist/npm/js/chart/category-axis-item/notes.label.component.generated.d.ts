import { ConfigurationService } from '../../common/configuration.service';
import { SettingsComponent } from '../../common/settings.component';
import { Border } from '../../common/property-types';
/**
 * @hidden
 */
export declare abstract class CategoryAxisNotesLabelComponentGenerated extends SettingsComponent {
    configurationService: ConfigurationService;
    /**
     * The background color of the label. Accepts a valid CSS color string, including hex and rgb.
     */
    background: string;
    /**
     * The border of the label.
     */
    border: Border;
    /**
     * The text color of the label. Accepts a valid CSS color string, including hex and rgb.
     */
    color: string;
    /**
     * The template which renders the labels.
     *
     * The field that can be used in the template is `value`. It defines the category value.
     *
     * The text can be split into multiple lines by using line feed characters (`"\n"`).
     */
    content: (e: any) => string;
    /**
     * The font style of the label.
     */
    font: string;
    /**
     * The format used to display the notes label. Uses `kendo.format`. Contains one placeholder (`"{0}"`)
     * which represents the category value.
     */
    format: string;
    /**
     * The position of the labels.
     *
     * - `"inside"`&mdash;The label is positioned inside the icon.
     * - `"outside"`&mdash;The label is positioned outside the icon.
     */
    position: string;
    /**
     * The rotation angle of the label. By default, the label are not rotated.
     */
    rotation: number;
    /**
     * If set to `true`, the Chart displays the category notes label. By default, the category notes label
     * is visible.
     */
    visible: boolean;
    constructor(configurationService: ConfigurationService);
}
