import { ConfigurationService } from '../../common/configuration.service';
import { SettingsComponent } from '../../common/settings.component';
import { AxisLabelVisualArgs, Border, DateFormats, LabelRotation } from '../../common/property-types';
import { Margin, Padding } from '../../common/property-types';
/**
 * @hidden
 */
export declare abstract class YAxisLabelsComponentGenerated extends SettingsComponent {
    configurationService: ConfigurationService;
    /**
     * The background color of the labels. Accepts a valid CSS color string, including hex and rgb.
     */
    background: string;
    /**
     * The border of the labels.
     */
    border: Border;
    /**
     * The text color of the labels. Accepts a valid CSS color string, including hex and rgb.
     */
    color: string;
    /**
     * The template which renders the labels.
     *
     * The field which can be used in the template is `value`. It defines the category value.
     *
     * The text can be split into multiple lines by using line feed characters (`"\n"`).
     */
    content: (e: any) => string;
    /**
     * The culture to use when formatting date values. For more information, refer to the article on globalization.
     */
    culture: string;
    /**
     * The format used to display the labels when the x values are dates. Uses `kendo.format`. Contains one
     * placeholder (`"{0}"`) which represents the category value.
     *
     * The Chart will choose the appropriate format for the current `yAxis.baseUnit`. Setting the
     * `categoryAxis.labels.format` option overrides the date formats.
     */
    dateFormats: DateFormats;
    /**
     * The font style of the labels.
     */
    font: string;
    /**
     * The format used to display the labels. Uses `kendo.format`. Contains one placeholder (`"{0}"`) which
     * represents the category value.
     */
    format: string;
    /**
     * The margin of the labels. A numeric value sets all margins.
     */
    margin: Margin | number;
    /**
     * If set to `true`, the Chart will mirror the axis labels and ticks. If the labels are normally on the
     * left side of the axis, the mirroring of the axis renders them to the right.
     */
    mirror: boolean;
    /**
     * The padding of the labels. A numeric value sets all paddings.
     */
    padding: Padding | number;
    /**
     * The rotation angle of the labels. By default, the labels are not rotated.
     */
    rotation: LabelRotation | 'any';
    /**
     * The number of labels to skip.
     */
    skip: number;
    /**
     * The label rendering step7mdash;renders every `n`<sup>th</sup> label. By default, every label is rendered.
     */
    step: number;
    /**
     * If set to `true`, the Chart displays the y-axis labels. By default, the y-axis labels are visible.
     */
    visible: boolean;
    /**
     * A function that can be used to create a custom visual for the labels.
     *
     * The available argument fields are:
     *
     * - `createVisual`&mdash;A function that can be used to get the default visual.
     * - `culture`&mdash;The default culture (if set) on the label.
     * - `format`&mdash;The default format of the label.
     * - `options`&mdash;The label options.
     * - `rect`&mdash;The `kendo.geometry.Rect` that defines where the visual has to be rendered.
     * - `sender`&mdash;The Chart instance (might be `undefined`).
     * - `text`&mdash;The label text.
     * - `value`&mdash;The category value.
     */
    visual: (e: AxisLabelVisualArgs) => void;
    constructor(configurationService: ConfigurationService);
}
