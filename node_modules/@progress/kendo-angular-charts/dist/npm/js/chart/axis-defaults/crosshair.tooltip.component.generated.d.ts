import { ConfigurationService } from '../../common/configuration.service';
import { SettingsComponent } from '../../common/settings.component';
import { Border, Padding } from '../../common/property-types';
/**
 * @hidden
 */
export declare abstract class AxisDefaultsCrosshairTooltipComponentGenerated extends SettingsComponent {
    configurationService: ConfigurationService;
    /**
     * The background color of the tooltip. Accepts a valid CSS color string, including hex and rgb.
     */
    background: string;
    /**
     * The border options.
     */
    border: Border;
    /**
     * The text color of the tooltip. Accepts a valid CSS color string, including hex and rgb.
     */
    color: string;
    /**
     * The tooltip font.
     */
    font: string;
    /**
     * The format used to display the tooltip. Uses `kendo.format`. Contains one placeholder (`"{0}"`) which
     * represents the category value.
     */
    format: string;
    /**
     * The padding of the crosshair tooltip. A numeric value sets all paddings.
     */
    padding: Padding | number;
    /**
     * If set to `true`, the chart displays the axis crosshair tooltip. By default, the axis crosshair
     * tooltip is not visible.
     */
    visible: boolean;
    constructor(configurationService: ConfigurationService);
}
