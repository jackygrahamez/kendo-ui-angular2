import { EventEmitter } from '@angular/core';
import { PagerSettings } from './pager-settings';
import { PageChangeEvent } from './change-event-args.interface';
/**
 * @hidden
 */
export declare class PagerComponent {
    total: number;
    skip: number;
    pageSize: number;
    options: PagerSettings | boolean;
    pageChange: EventEmitter<PageChangeEvent>;
    pagerWrapClass: boolean;
    gridPagerClass: boolean;
    widgetClass: boolean;
    settings: PagerSettings;
    prevButtonsDisabled: boolean;
    nextButtonsDisabled: boolean;
    showInitialPageSize: boolean;
    pageSizeChange(value: any): void;
    changePage(page: number): boolean;
    currentPage: number;
    totalPages: number;
    onInputChange(value: string): void;
    buttons: number[];
    private end;
    private maxItems;
    private start;
}
