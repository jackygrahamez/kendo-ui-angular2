import { AfterContentInit, ElementRef } from '@angular/core';
import { ThemeService } from '../common/theme.service';
/**
 * @hidden
 */
export declare class ChartAutoThemeComponent implements AfterContentInit {
    private element;
    private themeService;
    constructor(element: ElementRef, themeService: ThemeService);
    ngAfterContentInit(): void;
    private setColors();
    private setFonts();
    private setSeriesColors();
    private mapColor(key, varName);
    private queryStyle(varName);
    private notify(key, value);
}
